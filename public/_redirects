# Force www to apex (avoids duplicate-content split) — 301 permanent.
https://www.aralostudio.com/*  https://aralostudio.com/:splat  301!
http://www.aralostudio.com/*   https://aralostudio.com/:splat  301!

# Old domain → new domain — 301 permanent. Only fires when the legacy
# host is routed to this Netlify deploy.
https://jeremyhowardwebdesign.com/*  https://aralostudio.com/:splat  301!
http://jeremyhowardwebdesign.com/*   https://aralostudio.com/:splat  301!
https://www.jeremyhowardwebdesign.com/*  https://aralostudio.com/:splat  301!
http://www.jeremyhowardwebdesign.com/*   https://aralostudio.com/:splat  301!

# Consolidated thin city pages → stronger neighbor page — 301 permanent.
# Caldwell folded into Nampa (Canyon County), Kuna folded into Meridian.
# Must precede the SPA fallback below.
/web-design-caldwell      /web-design-nampa/     301!
/web-design-caldwell/*    /web-design-nampa/     301!
/web-design-kuna          /web-design-meridian/  301!
/web-design-kuna/*        /web-design-meridian/  301!

# First-party analytics proxy. Both lines must precede the SPA fallback, or
# /index.html swallows them.
#
# Why proxy instead of loading the script cross-origin: self-hosting alone does
# not beat uBlock Origin or Brave, because their filter lists match the
# /script.js path, not just the hostname. The upstream image is prebuilt, and
# TRACKER_SCRIPT_NAME is baked in at build time, so the renamed script is not
# served upstream and /site-utils.js 404s there. Proxying solves both: the
# browser only ever sees a same-origin request to an innocuous path, and the
# upstream host never appears in the page at all.
#
# The tracker derives its collect URL from its own script src directory, so a
# script served at /site-utils.js posts to /api/su on this same origin, which
# the second rule forwards. Renaming one without the other breaks collection.
/site-utils.js  https://assets.aralostudio.com/script.js  200
/api/su         https://assets.aralostudio.com/api/su     200

# Unknown paths serve the pre-rendered 404 page with a real 404 status.
#
# This was previously `/*  /index.html  200`, which returned the full home page
# for every typo'd or stale URL. Google treats that as a soft 404: it wastes
# crawl budget and reports as an error in Search Console.
#
# Safe because every route in the app is pre-rendered to its own index.html and
# is served before this rule is reached — the only route that is not
# pre-rendered is /404 itself. A route added to App.tsx but not to ROUTE_META
# in prerender.mjs will 404 here, which is the correct signal: an unrendered
# route has no indexable content anyway.
/*  /404.html  404
